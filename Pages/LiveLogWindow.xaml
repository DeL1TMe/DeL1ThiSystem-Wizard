<Window x:Class="DeL1ThiSystem.ConfigurationWizard.Pages.LiveLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Журнал"
        Width="720"
        Height="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            CornerRadius="14"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="0"/>
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <Style x:Key="ScrollBarCompact" TargetType="{x:Type ScrollBar}">
            <Setter Property="Width" Value="8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid Margin="0,8,3,8" Background="Transparent" Width="{TemplateBinding Width}">
                            <Border CornerRadius="4" Background="{DynamicResource Brush.ScrollTrack}" Margin="1,0,1,0"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineUpCommand" Opacity="0" IsHitTestVisible="False"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineDownCommand" Opacity="0" IsHitTestVisible="False"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                <Border x:Name="bd" CornerRadius="4" Background="{DynamicResource Brush.ScrollThumb}" Margin="1"/>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="bd" Property="Background" Value="{DynamicResource Brush.ScrollThumbHover}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter TargetName="bd" Property="Background" Value="{DynamicResource Brush.ScrollThumbDrag}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid x:Name="RootGrid" Background="Transparent" MouseLeftButtonDown="Root_MouseLeftButtonDown">
        <Viewbox x:Name="WindowScaler"
                 Stretch="Uniform"
                 StretchDirection="DownOnly"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center">
            <Border x:Name="WindowShell"
                    Width="720"
                    Height="800"
                    CornerRadius="14"
                    Background="{DynamicResource Brush.WindowBorder}"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    ClipToBounds="True"
                    SnapsToDevicePixels="True"
                    UseLayoutRounding="True">
                <Grid SnapsToDevicePixels="True" UseLayoutRounding="True">
                    <Border x:Name="WindowCard"
                            CornerRadius="13"
                            Background="{DynamicResource Brush.WindowBg}"
                            Margin="1"
                            ClipToBounds="True"
                            SnapsToDevicePixels="True"
                            UseLayoutRounding="True">
                        <Grid Margin="12">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="1"
                                        FontFamily="Segoe MDL2 Assets"
                                        Content="&#xE8BB;"
                                        Style="{StaticResource ButtonHeaderClose}"
                                        Click="Close_Click"/>
                            </Grid>

                            <Border Grid.Row="1"
                                    CornerRadius="12"
                                    Background="{DynamicResource Brush.PopupBg}"
                                    BorderBrush="{DynamicResource Brush.PopupBorder}"
                                    BorderThickness="1"
                                    ClipToBounds="True"
                                    Margin="2,2,5,2">
                                <Grid>
                                    <TextBox x:Name="LogTextBox"
                                             Margin="0"
                                             Padding="10,8"
                                             FontFamily="Consolas"
                                             FontSize="12"
                                             IsReadOnly="True"
                                             TextWrapping="Wrap"
                                             VerticalScrollBarVisibility="Auto"
                                             HorizontalScrollBarVisibility="Disabled"
                                             Background="Transparent"
                                             Foreground="{DynamicResource Brush.TextPrimary}"
                                             BorderThickness="0">
                                        <TextBox.Resources>
                                            <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource ScrollBarCompact}"/>
                                        </TextBox.Resources>
                                    </TextBox>

                                    <Border x:Name="TopFadeOverlay"
                                            Height="96"
                                            VerticalAlignment="Top"
                                            IsHitTestVisible="False"
                                            Background="{DynamicResource Brush.TopFade}"
                                            Opacity="1"
                                            Margin="0,0,18,0"
                                            CornerRadius="12,12,0,0"/>
                                    <Border x:Name="BottomFadeOverlay"
                                            Height="140"
                                            VerticalAlignment="Bottom"
                                            IsHitTestVisible="False"
                                            Background="{DynamicResource Brush.BottomFade}"
                                            Opacity="1"
                                            Margin="0,0,18,0"
                                            CornerRadius="0,0,12,12"/>
                                </Grid>
                            </Border>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Viewbox>
    </Grid>
</Window>
