<Page x:Class="DeL1ThiSystem.ConfigurationWizard.Pages.ProgressPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent">

    <Page.Resources>
        <Style x:Key="ProgressBlinkText" TargetType="TextBlock" BasedOn="{StaticResource TextBody}">
            <Setter Property="Foreground" Value="{DynamicResource Brush.TextSecondary}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                    <DataTrigger.EnterActions>
                        <BeginStoryboard x:Name="BlinkStoryboard">
                            <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 From="1" To="0.35" Duration="0:0:1.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <StopStoryboard BeginStoryboardName="BlinkStoryboard"/>
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ProgressRebootButton" TargetType="Button">
            <Setter Property="Foreground" Value="#E6FFFFFF"/>
            <Setter Property="Background" Value="#2A3345"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Brush.RebootButtonBorder}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="TextBlock.LineStackingStrategy" Value="MaxHeight"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="UseLayoutRounding" Value="True"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="cp"
                                              Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="True">
                                <Setter Property="Background" Value="{DynamicResource Brush.RebootButtonBg}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource Brush.RebootButtonBorderActive}"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsEnabled" Value="True"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" Value="{DynamicResource Brush.RebootButtonBgHover}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource Brush.RebootButtonBorderActive}"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsEnabled" Value="True"/>
                                    <Condition Property="IsPressed" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" Value="{DynamicResource Brush.RebootButtonBgPressed}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource Brush.RebootButtonBorderActive}"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="{DynamicResource Brush.RebootButtonBgDisabled}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource Brush.RebootButtonBorder}"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Foreground" Value="{DynamicResource Brush.RebootButtonTextDisabled}"/>
                                <Setter TargetName="cp" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LogIconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Background" Value="{DynamicResource Brush.SecondaryButtonBg}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Brush.SecondaryButtonBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Brush.SecondaryButtonBgHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Brush.SecondaryButtonBgPressed}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Brush.SecondaryButtonBgDisabled}"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource Brush.SecondaryButtonBorderDisabled}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid x:Name="ProgressContentRoot">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" VerticalAlignment="Center">
                <StackPanel Width="630" HorizontalAlignment="Center">
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,12">
                        <TextBlock x:Name="HeaderTextBlock"
                                   Style="{StaticResource TextHeading}"
                                   Text="{Binding HeaderText}"
                                   TextAlignment="Center"
                                   VerticalAlignment="Center"/>
                        <Button Style="{StaticResource LogIconButtonStyle}"
                                Margin="8,0,0,0"
                                ToolTip="Открыть журнал"
                                VerticalAlignment="Center"
                                Click="ToggleLog_Click">
                            <Viewbox Width="16" Height="16">
                                <Canvas Width="16" Height="16">
                                    <Rectangle Width="12" Height="14" RadiusX="1.5" RadiusY="1.5"
                                               Canvas.Left="2" Canvas.Top="1"
                                               Stroke="{DynamicResource Brush.TextPrimary}" Fill="Transparent" StrokeThickness="1.3"/>
                                    <Line X1="4.5" Y1="5" X2="11.5" Y2="5" Stroke="{DynamicResource Brush.TextPrimary}" StrokeThickness="1.2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                    <Line X1="4.5" Y1="8" X2="11.5" Y2="8" Stroke="{DynamicResource Brush.TextPrimary}" StrokeThickness="1.2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                    <Line X1="4.5" Y1="11" X2="9.5" Y2="11" Stroke="{DynamicResource Brush.TextPrimary}" StrokeThickness="1.2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                </Canvas>
                            </Viewbox>
                        </Button>
                    </StackPanel>

                    <TextBlock Style="{StaticResource ProgressBlinkText}"
                               Text="{Binding CurrentStepText}" TextAlignment="Center" Margin="0,0,0,12"/>

                    <Grid Margin="0,0,0,12" Width="630" Height="16" SnapsToDevicePixels="True" UseLayoutRounding="True"
                          Visibility="{Binding ProgressVisibility}">
                        <Border Width="630" Height="16"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                CornerRadius="6"
                                Background="{DynamicResource Brush.PopupBorder}"
                                ClipToBounds="True"
                                SnapsToDevicePixels="True"
                                UseLayoutRounding="True">
                            <Rectangle RadiusX="6" RadiusY="6"
                                       Fill="#0078D4"
                                       HorizontalAlignment="Left"
                                       Width="{Binding ProgressWidth}"
                                       SnapsToDevicePixels="True"
                                       UseLayoutRounding="True"/>
                        </Border>
                    </Grid>

                    <TextBlock Style="{StaticResource TextBody}"
                               Text="{Binding PercentText}" TextAlignment="Center" Opacity="0.9"
                               Margin="0,0,0,0"
                               Visibility="{Binding ProgressVisibility}"/>
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Background="{DynamicResource Brush.FooterBar}"
                    CornerRadius="0,0,16,16" Padding="18,12"
                    Visibility="{Binding FooterVisibility}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Style="{StaticResource TextFooterNote}"
                               Text="{Binding FooterText}" TextWrapping="Wrap" VerticalAlignment="Center"/>

                    <Button Grid.Column="1" Content="Понятно" Width="130" Height="34" Margin="8,0,0,0"
                            Style="{StaticResource ButtonPrimary}"
                            Visibility="{Binding FooterDismissVisible}"
                            Click="HideFooter_Click"/>

                    <Button Grid.Column="1" Content="Перезагрузить" Width="130" Height="34" Margin="0,0,0,0"
                            Style="{StaticResource ProgressRebootButton}"
                            Visibility="{Binding RebootVisible}"
                            IsEnabled="{Binding RebootEnabled}"
                            Click="Reboot_Click"/>
                </Grid>
            </Border>
        </Grid>

    </Grid>
</Page>
