<Window x:Class="DeL1ThiSystem.ConfigurationWizard.Pages.ProfileInitWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowState="Maximized"
        Topmost="True"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="#EE000000">
    <Grid>
        <Border Width="760"
                Height="220"
                Background="#FF111827"
                BorderBrush="#FF2B3A55"
                BorderThickness="1"
                CornerRadius="12"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Padding="28">
            <StackPanel>
                <TextBlock Text="Подготавливаем профиль пользователя"
                           Foreground="White"
                           FontSize="24"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8"
                           TextAlignment="Center"/>
                <TextBlock Text="{Binding CurrentStepText}"
                           Foreground="#D0D7E2"
                           FontSize="15"
                           Margin="0,0,0,16"
                           TextAlignment="Center"/>

                <Grid Height="16" Width="700">
                    <Border Background="#243247" CornerRadius="6"/>
                    <Border Background="#0078D4"
                            CornerRadius="6"
                            HorizontalAlignment="Left"
                            Width="{Binding ProgressWidth}"/>
                </Grid>

                <TextBlock Text="{Binding PercentText}"
                           Foreground="#D0D7E2"
                           FontSize="14"
                           Margin="0,12,0,0"
                           TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
